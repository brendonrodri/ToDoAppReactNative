import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    headerComponent: {
        flex: 1,
        alignItems: "center",
        backgroundColor: 'black',
        fontSize: 20,
        fontFamily: 'Arial'
    }
})